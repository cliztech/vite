name: CodeQL

on:
  schedule:
    - cron: '0 8 * * 1'
  pull_request:
    branches: ["**"]

jobs:
  analyze:
    permissions:
      security-events: write
      actions: read
      contents: read
    uses: github/codeql-action/.github/workflows/codeql.yml@v3
    with:
      languages: javascript

